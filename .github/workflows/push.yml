name: Test play

on:
  push:
    branches:
      - development

jobs:
  Go:
    runs-on: ubuntu-latest
    steps:
      - name: step-one
        shell: bash
        run: |
          echo "${{secrets.RELEASE_PLEASE_ACTION_TOKEN}}"
      - name: step-two
        shell: bash
        run: |
          echo "${{env.ENV}}"
          echo "${{vars}}"
          echo "${{github}}"
